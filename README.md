Project Overview
This repository contains automated test cases developed using Playwright for ITPM Assignment 01.

📌 All test scripts are located inside the tests/ folder.

Prerequisites
```Node.js (version 18 or later)
npm (comes with Node.js)
Installation Dependancies Instructions
Step 1: Clone the repository
git clone <repository-url>
cd IT23602328-ITPM-Assignment-1

## Step 2: Install dependencies
npm install

## Step 3: Install Playwright browsers
npx playwright install

## Running the Tests
npx playwright test
npx playwright test --ui
npx playwright show-report```
